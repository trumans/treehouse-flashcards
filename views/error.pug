extends layout

block body
  section.desk
      h1 #{error.message} - Status: #{error.statusCode}
      img.desk-items(src='/img/desk.svg')
      a.button-primary-small(href='/') Go Home
      .paper
        pre.stack-trace= error.stack
      .tear
